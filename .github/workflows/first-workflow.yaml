name: First Workflow
on: [push]

jobs:
    run-shell-commands:
        runs-on: ubuntu-latest
        steps:
            - name: echo a string
              run: echo "Hello Nagesh"
            - name: Multiline Command
              run: |
                node -v
                npm -v
            - name: Output
              run: echo "My First work flow completed"